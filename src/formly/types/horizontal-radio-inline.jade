label.radio-inline(ng-repeat="option in to.options")
  input(
    type="radio"
    id="{{id+'_'+$index}}"
    ng-value="option[to.valueProp||'value']"
    ng-model="model[options.key]")
  |  {{option[to.labelProp||'name']}}