mixin checkbox-inline(name,label,options)
  .form-group
    label.col-xs-2=label
    .col-xs-10: each option, key in options
      label.checkbox-inline
        input(type="checkbox" name=name value=key)&attributes(attributes)
        |  #{option}